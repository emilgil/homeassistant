  default_view:
    view: yes
    name: Default
    icon: mdi:home
    entities:
      - group.indoor_climate
      - group.outdoor_climate
      - group.weather_now
      - group.weather_tomorrow
      - group.media_players
      - group.switches
      - group.road_temp
      - group.air_temp

  computer_tab:
    name: Computers
    view: yes
    icon: mdi:docker
    entities:
      - group.docker_containers
      - group.computers
      - group.network

  automation_tab:
    name: Automations
    view: yes
    icon: mdi:wrench
    entities:
      - group.automations
      - group.radio
